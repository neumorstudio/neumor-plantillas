---
interface ClassItem {
  name: string;
  description: string;
  price: string;
  duration?: string;
  tag?: string;
  level?: string;
}

interface ClassCategory {
  name: string;
  icon: string;
  items: ClassItem[];
}

interface Props {
  title?: string;
  subtitle?: string;
  categories?: ClassCategory[];
}

const defaultCategories: ClassCategory[] = [
  {
    name: "Sesiones",
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" y1="8" x2="19" y2="14"/><line x1="22" y1="11" x2="16" y2="11"/></svg>`,
    items: [
      {
        name: "Entrenamiento Personal 1-on-1",
        description: "Sesion individual 100% personalizada a tus objetivos, nivel y disponibilidad",
        price: "40",
        duration: "60 min",
        tag: "popular",
        level: "Todos los niveles",
      },
      {
        name: "Sesion Duo (2 personas)",
        description: "Entrena con tu pareja o amigo/a y compartid el coste del entrenamiento",
        price: "30",
        duration: "60 min",
        tag: "popular",
      },
      {
        name: "Entrenamiento Online",
        description: "Sesion por videollamada con seguimiento en tiempo real desde casa",
        price: "30",
        duration: "60 min",
        tag: "nuevo",
      },
      {
        name: "Valoracion Inicial",
        description: "Evaluacion completa: medidas corporales, test de fuerza y plan de objetivos",
        price: "35",
        duration: "90 min",
        level: "Primera sesion",
      },
    ],
  },
  {
    name: "Paquetes",
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 16V8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16z"/><polyline points="3.27 6.96 12 12.01 20.73 6.96"/><line x1="12" y1="22.08" x2="12" y2="12"/></svg>`,
    items: [
      {
        name: "Bono 10 Sesiones",
        description: "Ahorra un 10% al contratar 10 sesiones individuales de entrenamiento",
        price: "360",
        duration: "10 sesiones",
        tag: "popular",
      },
      {
        name: "Bono 20 Sesiones",
        description: "Ahorra un 15% con nuestro paquete mas popular para resultados consistentes",
        price: "680",
        duration: "20 sesiones",
        tag: "popular",
      },
      {
        name: "Mensual Ilimitado",
        description: "Entrena todas las veces que quieras durante un mes completo",
        price: "299",
        duration: "1 mes",
        tag: "nuevo",
        level: "Mejor valor",
      },
      {
        name: "Trimestral Ilimitado",
        description: "Compromiso de 3 meses con precio reducido y seguimiento premium",
        price: "799",
        duration: "3 meses",
        level: "Ahorro maximo",
      },
    ],
  },
  {
    name: "Especialidades",
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>`,
    items: [
      {
        name: "Perdida de Peso",
        description: "Programa especifico para quemar grasa con HIIT, fuerza y seguimiento nutricional",
        price: "45",
        duration: "60 min",
        tag: "popular",
      },
      {
        name: "Ganancia Muscular",
        description: "Entrenamiento de hipertrofia con periodizacion y control de cargas",
        price: "45",
        duration: "75 min",
      },
      {
        name: "Preparacion Fisica",
        description: "Mejora tu rendimiento deportivo con entrenamiento especifico",
        price: "50",
        duration: "60 min",
      },
      {
        name: "Rehabilitacion",
        description: "Recuperacion de lesiones y vuelta al ejercicio de forma segura",
        price: "50",
        duration: "60 min",
        level: "Con informe medico",
      },
    ],
  },
  {
    name: "Online",
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>`,
    items: [
      {
        name: "Plan Mensual Online",
        description: "Planificacion semanal personalizada con videos y seguimiento por app",
        price: "79",
        duration: "1 mes",
        tag: "popular",
      },
      {
        name: "Consultoria Nutricional",
        description: "Plan alimenticio personalizado con recetas y lista de la compra",
        price: "60",
        duration: "Unico pago",
        tag: "nuevo",
      },
      {
        name: "Seguimiento Premium",
        description: "Soporte diario por WhatsApp + revision semanal de progreso",
        price: "149",
        duration: "1 mes",
        level: "Soporte 24/7",
      },
      {
        name: "Pack Completo Online",
        description: "Planificacion + Nutricion + Seguimiento todo incluido",
        price: "199",
        duration: "1 mes",
        tag: "popular",
        level: "Todo incluido",
      },
    ],
  },
];

const {
  title = "Servicios y Paquetes",
  subtitle = "Entrenamiento adaptado a tus objetivos y presupuesto",
  categories = defaultCategories,
} = Astro.props;
---

<section id="classes" class="classes-section">
  <div class="container">
    <div class="section-header">
      <h2 class="section-title">{title}</h2>
      <p class="section-subtitle">{subtitle}</p>
    </div>

    <div class="category-tabs">
      {categories.map((category, index) => (
        <button
          class={`category-tab neumor-btn ${index === 0 ? 'active' : ''}`}
          data-category={index}
        >
          <span class="tab-icon" set:html={category.icon} />
          <span class="tab-name">{category.name}</span>
        </button>
      ))}
    </div>

    <div class="classes-content">
      {categories.map((category, catIndex) => (
        <div class={`classes-category ${catIndex === 0 ? 'active' : ''}`} data-category-content={catIndex}>
          <div class="classes-grid">
            {category.items.map((item, itemIndex) => (
              <article class="class-item neumor-card" style={`animation-delay: ${itemIndex * 100}ms`}>
                <div class="item-content">
                  <div class="item-header">
                    <h3 class="item-name">{item.name}</h3>
                    {item.tag && <span class={`item-tag tag-${item.tag}`}>{item.tag}</span>}
                  </div>
                  <p class="item-description">{item.description}</p>
                  <div class="item-meta">
                    {item.duration && (
                      <span class="item-duration">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="meta-icon">
                          <circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/>
                        </svg>
                        {item.duration}
                      </span>
                    )}
                    {item.level && (
                      <span class="item-level">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" class="meta-icon">
                          <path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/>
                        </svg>
                        {item.level}
                      </span>
                    )}
                  </div>
                </div>
                <div class="item-price">
                  <span class="price-value">{item.price}</span>
                  <span class="price-currency">€</span>
                </div>
              </article>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .classes-section { padding: 6rem 0; }
  .section-header { text-align: center; margin-bottom: 3rem; }
  .section-title { font-size: clamp(2rem, 5vw, 3rem); font-weight: 700; margin-bottom: 0.75rem; color: var(--text-primary); }
  .section-subtitle { font-size: 1.1rem; color: var(--text-secondary); max-width: 500px; margin: 0 auto; }
  .category-tabs { display: flex; justify-content: center; gap: 1rem; margin-bottom: 3rem; flex-wrap: wrap; }
  .category-tab { display: flex; align-items: center; gap: 0.5rem; padding: 0.75rem 1.5rem; border-radius: 1rem; font-weight: 500; transition: all 0.3s ease; cursor: pointer; }
  .category-tab.active { background: var(--accent); color: white; box-shadow: 4px 4px 12px var(--shadow-dark), -2px -2px 8px var(--shadow-light); }
  .tab-icon { width: 20px; height: 20px; display: flex; }
  .tab-icon :global(svg) { width: 100%; height: 100%; }
  .tab-name { font-size: 0.95rem; }
  .classes-content { position: relative; }
  .classes-category { display: none; animation: fadeIn 0.4s ease; }
  .classes-category.active { display: block; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
  .classes-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 1.5rem; }
  .class-item { display: flex; justify-content: space-between; align-items: flex-start; padding: 1.5rem; border-radius: 1.25rem; transition: transform 0.3s ease; animation: slideUp 0.5s ease backwards; }
  @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  .class-item:hover { transform: translateY(-4px); }
  .item-content { flex: 1; padding-right: 1rem; }
  .item-header { display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.5rem; flex-wrap: wrap; }
  .item-name { font-size: 1.1rem; font-weight: 600; color: var(--text-primary); margin: 0; }
  .item-tag { font-size: 0.7rem; font-weight: 600; text-transform: uppercase; padding: 0.25rem 0.6rem; border-radius: 2rem; }
  .tag-popular { background: linear-gradient(135deg, #f59e0b, #d97706); color: white; }
  .tag-nuevo { background: linear-gradient(135deg, #10b981, #059669); color: white; }
  .item-description { font-size: 0.9rem; color: var(--text-secondary); line-height: 1.5; margin: 0 0 0.5rem 0; }
  .item-meta { display: flex; flex-wrap: wrap; gap: 1rem; }
  .item-duration, .item-level { display: inline-flex; align-items: center; gap: 0.25rem; font-size: 0.8rem; color: var(--text-secondary); }
  .meta-icon { width: 14px; height: 14px; }
  .item-price { display: flex; align-items: baseline; padding: 0.75rem 1rem; border-radius: 1rem; background: var(--neumor-bg); box-shadow: inset 3px 3px 6px var(--shadow-dark), inset -3px -3px 6px var(--shadow-light); }
  .price-value { font-size: 1.4rem; font-weight: 700; color: var(--text-primary); }
  .price-currency { font-size: 0.9rem; font-weight: 500; color: var(--accent); margin-left: 2px; }
  @media (max-width: 768px) {
    .classes-section { padding: 4rem 0; }
    .category-tabs { gap: 0.75rem; }
    .category-tab { padding: 0.6rem 1rem; }
    .tab-name { display: none; }
    .tab-icon { width: 24px; height: 24px; }
    .category-tab.active .tab-name { display: inline; }
    .classes-grid { grid-template-columns: 1fr; }
    .class-item { flex-direction: column; gap: 1rem; }
    .item-content { padding-right: 0; }
    .item-price { align-self: flex-start; }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const tabs = document.querySelectorAll('.category-tab');
    const contents = document.querySelectorAll('.classes-category');
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        const categoryIndex = tab.getAttribute('data-category');
        tabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        contents.forEach(content => {
          content.classList.remove('active');
          if (content.getAttribute('data-category-content') === categoryIndex) content.classList.add('active');
        });
      });
    });
  });
</script>
